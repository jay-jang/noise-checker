# 변형 규칙 FP 시뮬레이션 — v1

> 생성: `scripts/variant_fp_sim.py` (재현 가능, 결정적).
> 일반어 코퍼스: 한국어 고빈도 일상어 319개 — 출처: 자체 선정(혐오/욕설 배제).

## 1. 입력

- 시드 용어(surface) 1개: `운지`
- 일반어 코퍼스: 319개

## 2. 변형 종류별 충돌 통계

변형의 normalized_key가 일반어의 normalized_key와 일치하면 충돌(=오탐 위험).

| kind | 생성 변형 수 | 충돌 수 | 충돌률 |
|------|------------:|--------:|------:|
| jamo | 1 | 0 | 0.0% |
| chosung | 1 | 0 | 0.0% |
| leet | 11 | 0 | 0.0% |
| **합계** | **13** | **0** | **0.0%** |

## 3. chosung 초성 길이별 충돌 (운영 권고 근거)

| 초성 길이 | chosung 변형 수 | 충돌 수 | 충돌률 |
|----------:|---------------:|--------:|------:|
| 2자 | 1 | 0 | 0.0% |

## 4. 충돌 쌍 목록 (시드 용어 변형 ↔ 일반어)

_시드 용어 변형과 일반어 간 직접 충돌 없음._ (현 시드는 동음이의 처리된 `운지` 단일 — 시드 수가 적어 직접 충돌 0은 정상. chosung 본질 위험은 아래 §5 코퍼스 자체 모호성 측정으로 입증.)

## 5. chosung 본질 모호성 — 일반어 코퍼스 자체 측정

시드 수와 무관하게 chosung 위험을 입증하기 위해, **일반어 코퍼스 자체를** chosung으로 환원해 서로 다른 일반어가 같은 초성 키로 뭉치는 정도를 잰다. 한 키에 2개 이상 일반어가 모이면, 그 키를 단독 매칭에 쓸 때 그 일반어 전부가 오탐 후보다(= 용어가 그 초성을 가지면 무해어를 잡는다).

| 초성 길이 | 일반어 수 | 같은 키 공유(≥2) 단어 수 | 모호 비율 |
|----------:|----------:|------------------------:|---------:|
| 1자 | 3 | 0 | 0.0% |
| 2자 | 273 | 219 | 80.2% |
| 3자 | 39 | 0 | 0.0% |
| 4자 | 4 | 0 | 0.0% |

대표 충돌 키(같은 초성을 공유하는 무해 일반어 묶음):

- `ㄱㅈ` (2자): 가족, 감자, 과자, 극장, 광장, 걱정, 결정, 경제, 검정, 기자
- `ㅇㅅ` (2자): 이슬, 약속, 우산, 열쇠, 예술, 역사, 우승, 의사, 운수
- `ㅇㄱ` (2자): 얼굴, 약국, 안개, 안경, 용기, 연극, 약간
- `ㅇㅈ` (2자): 여자, 어제, 의자, 옷장, 아주, 아직, 운전
- `ㅎㅇ` (2자): 혈압, 휴일, 회의, 회원, 하양, 할인, 행운
- `ㅅㄱ` (2자): 사과, 산길, 시계, 수건, 성공, 생각
- `ㅈㄱ` (2자): 장갑, 지갑, 종교, 조금, 작가, 지구
- `ㄱㄱ` (2자): 건강, 감기, 경기, 가게, 가격

## 6. 운영 권고

1. **chosung 2자 이하는 단독 매칭 금지(문맥 필요).** 초성 2자(예 `ㅅㅂ`, `ㅇㅈ`)는 무해한 일상어·약어와 충돌 가능성이 높다(§5의 코퍼스 자체 모호성 측정에서 2자 키가 다수 일반어를 공유함을 확인). chosung 변형은 `verified=false` 기본, 단독 1차 매칭에서 제외하고 문맥 분류 단계로만 승격한다(또는 2자 이하 chosung은 적재하되 release 게이트에서 비활성). 시드 데이터의 `운지` 엔트리도 chosung `ㅇㅈ`를 '문맥 의존'으로 명시한다.
2. **leet 중성 덧댐형(예 `운지1`)은 키가 일반어와 충돌하지 않으나** 과생성 위험이 있으므로 용어당 상한(≤20)을 유지하고 `auto_generated=true, verified=false`로 적재해 검수 큐에 넣는다.
3. **jamo 변형은 정의상 surface와 같은 키로 수렴**하므로 일반어와의 충돌은 곧 '해당 surface 자체가 일반어와 동음'임을 의미한다(동음이의어 신호). 이 경우 변형이 아니라 surface의 `ambiguity` 재검토 대상이다.
4. 충돌이 1건이라도 발생한 변형 규칙·종류는 **자동 적재 시 verified=false 강제 + 검수 필수**. 임계(예: 종류별 충돌률 > 0%) 초과 종류는 release 게이트에서 단독 매칭 비활성을 기본값으로 둔다.

> 주: 본 코퍼스는 소규모 자체 선정이므로 절대 충돌률보다 **종류 간 상대 위험(특히 chosung ≫ leet·jamo)** 해석에 사용한다. 대규모 코퍼스(모두의 말뭉치 등) 적용은 후속 과제.
